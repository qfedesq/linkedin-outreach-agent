DATABASE_URL="file:./dev.db"

# Auth (NextAuth)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Encryption key for credentials (32 hex bytes — generate with: openssl rand -hex 32)
ENCRYPTION_KEY=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Dev only — set to "true" to bypass auth for local preview
DEV_BYPASS_AUTH="false"
